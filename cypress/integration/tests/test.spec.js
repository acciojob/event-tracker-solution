/// <reference types="Cypress"/>

describe("test suite name", () => {

  const baseURL = "http://localhost:8080";
  
  beforeEach(() => {
    cy.visit(baseURL);
  });

  it("test case name", () => {


  });

});
